1、类可以允许其他类或者函数访问它的非公有成员，方法是另其他类或者函数成为它的友元。
<1>把一个函数作为类的友元，只需要在类中增加一条以friend关键字开始的函数声明语句。
<2>友元声明只能出现在类定义的内部，但是类内出现的具体位置不限。

2、如果希望类的用户能够调用某个友元函数，那么我们就必须在友元声明之外再专门对函数进行一次声明。
<1>类的外部，将友元的声明与类本身放置在同一个头文件中；
<2>区别与类内部的友元声明。