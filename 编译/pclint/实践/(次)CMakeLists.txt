INCLUDE_DIRECTORIES(
	${3RDPARTY}/gbase/include
	./include
	./pub)

LINK_DIRECTORIES(
	${3RDPARTY}/gbase/lib)

AUX_SOURCE_DIRECTORY(./source SRC_FILES)

SET(LIB_NAME log_server)
SET(EXE_NAME RCP_LOG_SERVER_${OS_TYPE}_${VER_TYPE}_${APP_VER})

IF(LINT)
	ADD_LINT(${LIB_NAME} ${SRC_FILES})
ELSE()
	ADD_LIBRARY(${LIB_NAME} STATIC ${SRC_FILES})
	ADD_EXECUTABLE(${EXE_NAME} ${SRC_FILES})
	TARGET_LINK_LIBRARIES(${EXE_NAME} gbaseclient)
ENDIF()